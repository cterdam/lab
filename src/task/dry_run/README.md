# dry_run

No-op task. Does nothing. Useful for verifying that the pipeline starts and
stops cleanly — the Docker image builds, Redis is healthy, arguments parse,
and the logger initializes without errors.
