"""Demo package: the shape every lab package follows (src/ + test/)."""


def greet(name: str) -> str:
    """A placeholder function so the skeleton has something to test."""
    return f"Hello, {name}!"
